version: '2'

services:
  etl:
    depends_on:
      - mysql
    links:
      - mysql:mysql

  web:
    depends_on:
      - mysql
    links:
      - mysql:mysql

  nginx:
    build:
      args:
        deployment_environment: production
    ports:
      - "443:443"